@if (TempData["success"] != null)
{
    <div class="alert alert-success md-margin-top">@Html.Raw(TempData["success"])</div>
} 
else if (TempData["error"] != null)
{
    <div class="alert alert-danger md-margin-top">@Html.Raw(TempData["error"])</div>
}

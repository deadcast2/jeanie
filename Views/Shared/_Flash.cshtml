@if (TempData["success"] != null)
{
    <div class="alert alert-success md-margin">@TempData["success"]</div>
} 
else if (TempData["error"] != null)
{
    <div class="alert alert-danger md-margin">@TempData["error"]</div>
}

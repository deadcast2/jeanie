@if (TempData["success"] != null)
{
    <div class="alert alert-success md-margin">@Html.Raw(TempData["success"])</div>
} 
else if (TempData["error"] != null)
{
    <div class="alert alert-danger md-margin">@Html.Raw(TempData["error"])</div>
}

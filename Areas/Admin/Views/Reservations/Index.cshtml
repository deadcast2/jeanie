@model (jeanie.Models.ReservationViewModel, List<jeanie.Models.ReservationViewModel>)
@{
    ViewBag.Title = "Admin";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

<div class="md-margin">
    <div class="row">
        <div class="col-lg-4">
            <form method="post" action="@Url.Action("create")">
                @Html.AntiForgeryToken()
                @Html.Partial("_FormErrors", Model.Item1)
                <div class="form-group">
                    <label>Name</label>
                    <input class="form-control" name="name" value="@Model.Item1.Name" placeholder="Required" />
                </div>
                <button type="submit" class="btn btn-success">
                    <i class="glyphicon glyphicon-plus"></i> Add Reservation
                </button>
            </form>
        </div>
        <div class="col-lg-8">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Grade</th>
                        <th>Time Slot</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var reservation in Model.Item2)
                    {
                        <tr>
                            <td>@reservation.Name</td>
                            <td>@reservation.Grade</td>
                            <td>@reservation.FormattedTimeSlot</td>
                            <td>
                                <form method="post" action="@Url.Action("Destroy")">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@reservation.Id" />
                                    <a href="@Url.Action("Edit", new { id = reservation.Id })" class="btn btn-default btn-xs">Edit</a>
                                    <a href="@reservation.Url" class="btn btn-default btn-xs copy" data-toggle="tooltip" data-placement="top"
                                       title="Copied to clipboard!">Copy Link</a>
                                    <a href="#" class="btn btn-danger btn-xs delete-confirm">Delete</a>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
